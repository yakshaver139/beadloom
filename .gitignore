# Binary (only the root binary, not cmd/beadloom/ source)
/beadloom

# Beads local database
.beads/

# IDE / editor
.vscode/

# Claude Code local settings
.claude/

# Node build artifacts (source project, not the committed dist)
beadloom_visualiser/dist/
